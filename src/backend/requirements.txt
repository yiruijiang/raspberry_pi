# Baby Monitor — Backend Python dependencies
# Python 3.11+ required (tested on Raspberry Pi OS Bookworm arm64)
#
# Install on Pi:
#   pip install -r requirements.txt
#
# Note: picamera2 is pre-installed on Pi OS Bookworm and is NOT installable
# via pip on non-Pi platforms.  On a dev machine, opencv-python provides the
# fallback V4L2 capture path.

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1        # includes websockets + httptools for performance

# ── Settings / config ──────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1

# ── Authentication ─────────────────────────────────────────────────────────
PyJWT==2.9.0                     # JWT encode/decode (HS256/RS256)

# ── Image encoding (MJPEG path via PIL when picamera2 yields RGB arrays) ──
Pillow==11.0.0

# ── Camera fallback for dev / non-Pi environments ─────────────────────────
# On Pi, picamera2 is used instead (system package, not pip).
# Uncomment the line below when developing on a laptop:
# opencv-python==4.10.0.84

# ── Audio capture ──────────────────────────────────────────────────────────
# pyaudio requires PortAudio headers.
# Pi:       sudo apt install -y portaudio19-dev python3-pyaudio
# macOS:    brew install portaudio && pip install pyaudio
# Linux CI: sudo apt install -y portaudio19-dev
pyaudio==0.2.14

# ── Async helpers ──────────────────────────────────────────────────────────
anyio==4.6.2                     # asyncio compatibility shim (FastAPI dep)

# ── Development / testing (not required on Pi for runtime) ────────────────
# pytest==8.3.3
# pytest-asyncio==0.24.0
# httpx==0.27.2                  # ASGI test client
# pytest-cov==6.0.0
